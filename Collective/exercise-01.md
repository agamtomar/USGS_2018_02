# Collective Communication: Exercise 2 Barrier

- Modify the program `mpi_barrier.{f90,cc,py,R}` in the Examples
  directory so that the MPI tasks print their `hello` message in
  ascending order.

# Solution
